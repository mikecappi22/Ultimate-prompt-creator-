se');return cleanJson(content)}catch(e){clearTimeout(timer);last=e;if(e.name==='AbortError'&&controller!==ac)throw e;if(attempt===2)throw last}}}
function rawPrompt(){return `${$('goal').value.trim()}\n${$('extra').value.trim()?`Extra focus: ${$('extra').value.trim()}\n`:''}\nExtract compact visual facts only. Cover every relevant section from the supplied schema. Keep each fact atomic and specific. Confidence must reflect what is actually visible. Do not identify the person, infer biography, or invent hidden details. Return JSON only.`}
function synthPrompt(raw){return `You are a forensic image prompt engineer. Convert the following visually grounded extraction into clean, atomic keyword records and reconstruction phrases. Do not add facts that are not supported by the extraction. Preserve confidence labels. Produce useful AI-image prompt wording, not generic quality spam.\n\nRAW VISUAL EXTRACTION:\n${JSON.stringify(raw)}\n\nReturn JSON only.`}
function itemId(si,ii){return si+'-'+ii}function usable(x){return x.confidence!=='CANNOT_VERIFY'&&(x.prompt_phrase||x.keyword)}function render(){selected.clear();$('results').classList.remove('hidden');$('summary').value=result.image_summary||'';$('reconstruction').value=result.reconstruction_summary||'';$('json').value=JSON.stringify(result,null,2);const root=$('sections');root.innerHTML='';(result.sections||[]).forEach((sec,si)=>{const box=document.createElement('div');box.className='section';box.innerHTML=`<h3>${sec.section.replaceAll('_',' ')}</h3><div class="small">${sec.summary||''}</div><div class="bubbles"></div>`;const w=box.querySelector('.bubbles');(sec.items||[]).forEach((it,ii)=>{const b=document.createElement('button'),id=itemId(si,ii);b.className='bubble';b.dataset.conf=it.confidence;b.dataset.id=id;b.textContent=it.keyword||it.prompt_phrase;b.title=`${it.description||''}\n${it.evidence||''}`;b.onclick=()=>{selected.has(id)?selected.delete(id):usable(it)&&selected.add(id);b.classList.toggle('selected',selected.has(id));rebuild()};w.appendChild(b);if(['CLEARLY_VISIBLE','HIGHLY_PROBABLE'].includes(it.confidence)&&usable(it))selected.add(id)});root.appendChild(box)});sync();rebuild()}
function sync(){qa('.bubble').forEach(b=>b.classList.toggle('selected',selected.has(b.dataset.id)))}function selectedItems(){const a=[];(result?.sections||[]).forEach((s,si)=>(s.items||[]).forEach((it,ii)=>selected.has(itemId(si,ii))&&a.push(it)));return a}function rebuild(){const seen=new Set(),p=[];for(const it of selectedItems()){const x=(it.prompt_phrase||it.keyword||'').trim(),k=x.toLowerCase();if(x&&!seen.has(k)){seen.add(k);p.push(x)}}$('selectedPrompt').value=p.join(', ')}
async function run(){if(!file||!connected)return;const cfg=profile();$('run').disabled=true;$('cancel').disabled=false;[0,1,2].forEach(i=>stage(i,'idle','waiting'));bar(2);setStatus('Preparing image…');try{imgB64=await compressImage(file);bar(15);stage(1,'run',`one image pass with ${$('visionModel').value}`);setStatus('Extracting visual facts from the image…');const raw=await streamCall({model:$('visionModel').value,schema:RAW_SCHEMA,prompt:rawPrompt(),images:[imgB64],tokens:cfg.visionTokens,timeout:cfg.visionTimeout,temperature:.12,ctx:cfg.ctx});stage(1,'done',`${(raw.sections||[]).reduce((n,s)=>n+(s.facts||[]).length,0)} visual facts`);bar(58);stage(2,'run',`text-only synthesis with ${$('textModel').value}`);setStatus('Organizing facts into prompt-ready keywords…');result=await streamCall({model:$('textModel').value,schema:SYNTH_SCHEMA,prompt:synthPrompt(raw),tokens:cfg.textTokens,timeout:cfg.textTimeout,temperature:.2,ctx:cfg.ctx});stage(2,'done',`${(result.sections||[]).reduce((n,s)=>n+(s.items||[]).length,0)} keyword items`);bar(100);setStatus('Fast forensic analysis complete.','good');render()}catch(e){if(e.name==='AbortError')setStatus('Analysis cancelled.');else setStatus('Analysis stopped: '+e.message,'bad');bar(0)}finally{$('run').disabled=!connected||!file;$('cancel').disabled=true;controller=null;setTimeout(()=>bar(0),700)}}
$('profile').onchange=chooseModels;$('check').onclick=check;$('warm').onclick=warm;$('installTurbo').onclick=installTurbo;$('file').onchange=async e=>{file=e.target.files?.[0]||null;imgB64='';const p=$('preview');p.innerHTML='';if(!file){$('run').disabled=true;return}const img=new Image();img.src=URL.createObjectURL(file);await img.decode();p.appendChild(img);$('run').disabled=!connected;setStatus('Image ready.')};$('run').onclick=run;$('cancel').onclick=()=>controller?.abort();$('selectHigh').onclick=()=>{selected.clear();(result?.sections||[]).forEach((s,si)=>(s.items||[]).forEach((it,ii)=>{if(['CLEARLY_VISIBLE','HIGHLY_PROBABLE'].includes(it.confidence)&&usable(it))selected.add(itemId(si,ii))}));sync();rebuild()};$('selectAll').onclick=()=>{selected.clear();(result?.sections||[]).forEach((s,si)=>(s.items||[]).forEach((it,ii)=>usable(it)&&selected.add(itemId(si,ii))));sync();rebuild()};$('clearSel').onclick=()=>{selected.clear();sync();rebuild()};$('rebuild').onclick=rebuild;$('copyKeywords').onclick=()=>navigator.clipboard.writeText(selectedItems().map(x=>x.keyword).filter(Boolean).join(', '));$('copyPhrases').onclick=()=>navigator.clipboard.writeText(selectedItems().map(x=>x.prompt_phrase).filter(Boolean).join(', '));$('copyPrompt').onclick=()=>navigator.clipboard.writeText($('selectedPrompt').value);$('sendMain').onclick=()=>{rebuild();const text=$('selectedPrompt').value.trim();if(!text)return;const payload={type:'append-to-prompt',text,source:'vision-lab-v116',ts:Date.now()};try{channel?.postMessage(payload)}catch(_){}try{localStorage.setItem('up