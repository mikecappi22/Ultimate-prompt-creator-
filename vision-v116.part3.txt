m;s.style.color=c==='bad'?'#b91c1c':c==='good'?'#15803d':''}function stage(i,state,msg){const r=$('st'+i);r.dataset.state=state;r.querySelector('b').textContent=state==='done'?'✓':state==='run'?'●':state==='retry'?'↻':state==='fail'?'!':'○';r.querySelector('span').textContent=msg}function bar(n){$('bar').style.width=Math.max(0,Math.min(100,n))+'%'}function cleanJson(s){s=String(s||'').trim();if(s.startsWith('```'))s=s.replace(/^```(?:json)?\s*/i,'').replace(/\s*```$/,'');const a=s.indexOf('{'),b=s.lastIndexOf('}');if(a>=0&&b>a)s=s.slice(a,b+1);return JSON.parse(s)}
function chooseModels(){const p=$('profile').value,vision=$('visionModel'),text=$('textModel');const visPref=p==='turbo'?['qwen3-vl:2b-instruct','qwen3-vl:2b']:['qwen3-vl:4b-instruct','qwen3-vl:4b','qwen3-vl:2b-instruct'];const txtPref=p==='turbo'?['qwen3:1.7b','qwen3:4b']:['qwen3:4b','qwen3:1.7b'];const v=visPref.find(x=>installed.includes(x))||installed.find(x=>/qwen3-vl/i.test(x))||vision.value;const t=txtPref.find(x=>installed.includes(x))||installed.find(x=>/^qwen3:(?!vl)/i.test(x))||v;vision.value=v;text.value=t;$('imageInfo').value=`${p[0].toUpperCase()+p.slice(1)}: max ${profile().px}px, compressed JPEG`;}
async function check(){try{$('conn').textContent='Checking Ollama…';const r=await fetch(endpoint()+'/api/tags',{cache:'no-store'});if(!r.ok)throw new Error('HTTP '+r.status);const j=await r.json();installed=(j.models||[]).map(x=>x.name||x.model).filter(Boolean);for(const [id,rx] of [['visionModel',/qwen3-vl|gemma3|llava/i],['textModel',/^qwen3:(?!.*vl)|gemma3/i]]){const s=$(id),cur=s.value,names=installed.filter(n=>rx.test(n));s.innerHTML='';for(const n of (names.length?names:installed))s.add(new Option(n,n));if(names.includes(cur))s.value=cur;}chooseModels();connected=true;$('conn').textContent=`Connected · ${installed.length} installed model(s). Vision: ${$('visionModel').value} · Text: ${$('textModel').value}`;$('run').disabled=!file;return true}catch(e){connected=false;$('conn').textContent='Cannot reach Ollama: '+e.message;return false}}
async function pullModel(name){const r=await fetch(endpoint()+'/api/pull',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({model:name,stream:true})});if(!r.ok)throw new Error('pull HTTP '+r.status);const rd=r.body?.getReader();if(!rd)return;const dec=new TextDecoder();let buf='';while(true){const {value,done}=await rd.read();if(done)break;buf+=dec.decode(value,{stream:true});const lines=buf.split('\n');buf=lines.pop()||'';for(const line of lines){if(!line.trim())continue;try{const j=JSON.parse(line);$('conn').textContent=`Installing ${name}: ${j.status||'working'}${j.total&&j.completed?` · ${Math.round(j.completed/j.total*100)}%`:''}`}catch(_){}}}}
async function installTurbo(){try{$('installTurbo').disabled=true;await pullModel('qwen3-vl:2b-instruct');await pullModel('qwen3:1.7b');await check();$('conn').textContent='Turbo models installed and ready.'}catch(e){$('conn').textContent='Turbo model install failed: '+e.message}finally{$('installTurbo').disabled=false}}
async function warmOne(model){const r=await fetch(endpoint()+'/api/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({model,stream:false,think:false,keep_alive:'30m',options:{num_predict:4,num_ctx:2048},messages:[{role:'user',content:'Reply READY.'}]})});if(!r.ok)throw new Error('warm HTTP '+r.status)}async function warm(){try{$('conn').textContent='Warming models…';await warmOne($('visionModel').value);if($('textModel').value!==$('visionModel').value)await warmOne($('textModel').value);$('conn').textContent='Models warm and kept alive for 30 minutes.'}catch(e){$('conn').textContent='Warm-up failed: '+e.message}}
async function compressImage(f){stage(0,'run','resizing and compressing locally');const img=new Image(),url=URL.createObjectURL(f);img.src=url;await img.decode();const p=profile(),sc=Math.min(1,p.px/Math.max(img.naturalWidth,img.naturalHeight)),w=Math.max(1,Math.round(img.naturalWidth*sc)),h=Math.max(1,Math.round(img.naturalHeight*sc)),cv=document.createElement('canvas');cv.width=w;cv.height=h;cv.getContext('2d').drawImage(img,0,0,w,h);URL.revokeObjectURL(url);const data=cv.toDataURL('image/jpeg',p.q);stage(0,'done',`${w}×${h} JPEG · ${Math.round(data.length*0.75/1024)} KB`);return data.split(',')[1]}
async function streamCall({model,schema,prompt,images,tokens,timeout,temperature=.2,ctx}){let last;for(let attempt=1;attempt<=2;attempt++){const ac=new AbortController();controller=ac;const limit=attempt===1?tokens:Math.max(900,Math.round(tokens*.6)),timer=setTimeout(()=>ac.abort(),timeout);try{const body={model,stream:true,format:schema,think:false,keep_alive:'30m',options:{temperature,top_p:.9,num_predict:limit,num_ctx:ctx},messages:[{role:'user',content:prompt,...(images?{images}: {})}]};const r=await fetch(endpoint()+'/api/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body),signal:ac.signal});if(!r.ok)throw new Error('Ollama HTTP '+r.status);const rd=r.body?.getReader();if(!rd){const j=await r.json();clearTimeout(timer);return cleanJson(j.message?.content||'')}const dec=new TextDecoder();let buf='',content='';while(true){const {value,done}=await rd.read();if(done)break;buf+=dec.decode(value,{stream:true});const lines=buf.split('\n');buf=lines.pop()||'';for(const line of lines){if(!line.trim())continue;try{const j=JSON.parse(line);content+=j.message?.content||''}catch(_){}}}if(buf.trim())try{content+=JSON.parse(buf).message?.content||''}catch(_){}clearTimeout(timer);if(!content.trim())throw new Error('empty respon